#include "main.h"
/*
 * infinite_add - functions that add two numbers
 * @n1: first number
 * @n2: second number
 * @r: buffer that stores the result
 * @size_r: buffer size
 * */

char *infinite_add(char *n1, char *n2, char *r, int size_r)
{
    
    return (r);
}
