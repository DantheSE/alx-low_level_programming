#include "main.h"
#include <stdio.h>
/**
 * mul - takes two integers and multiplies them
 * @a: first integer to be collected
 * @b: second integer to be collected
 * Return: result of multiplication of the two integers
 */

int mul(int a, int b)
{
return (a * b);
}
